git init
git config --global user.name "这里就上图中圈着的name"
git config --global user.email "这里是上图中圈着的邮箱"
git remote add origin git@github.com:yourName/yourRepo.git
git add README
git commit -m "代码提交"
git push origin master
